This project was created using Python and Django.
To run the server, run the following command exlcuding the quotation marks: "python manage.py runserver 8080"
  if that command does not work, try "python3 manage.py runserver 8080"
